import readline from "read-console-input";

const name = readline("What's your name: ");
console.log("Hello " + name);

const a = Number(readline("Number 1: "));
const b = Number(readline("Number 2: "));
console.log(a + b);